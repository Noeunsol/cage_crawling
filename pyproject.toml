[project]
name = "taxonomy-crawler"
version = "0.1.0"
description = "Taxonomy 기반 한국 웹 콘텐츠 Focused Crawler (mock end-to-end skeleton)"
requires-python = ">=3.10"
dependencies = ["pyyaml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
